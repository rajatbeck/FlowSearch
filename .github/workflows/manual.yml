name: Manually workflow
on:
  workflow_dispatch:
    inputs:
      flavours:
        description: 'flavours'
        required: true
        default: 'debug'
      tags:
        description: 'Test scenario tags'
jobs:
  printInputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Log level: ${{ github.event.inputs.flavours }}"
          echo "Tags: ${{ github.event.inputs.tags }}"